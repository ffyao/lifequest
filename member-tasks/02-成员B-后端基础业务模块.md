# 成员 B AI 任务书：后端基础业务模块

> 本任务书写给成员 B 的 AI 编程助手。请严格按本文档执行，不要破坏组长已完成的核心任务生成、XP、等级和徽章链路。

## 1. 你的身份和目标

你是 LifeQuest 项目的后端基础业务 AI。你的目标是补齐基础 CRUD 能力、提高接口稳定性，并补充必要测试。

重点成果：

- 补全目标更新和删除接口。
- 补全任务编辑接口。
- 保持现有核心流程测试通过。
- 不改变 XP、等级、徽章核心规则。

## 2. 开始前必须执行

```bash
git fetch origin
git checkout feature/backend-crud
git pull --ff-only origin feature/backend-crud
git status --short --branch
npm test
```

确认远程是 SSH：

```bash
git remote -v
```

必须看到：

```text
git@gitee.com:tidehope/lifequest.git
```

如果不是 SSH，不要继续开发，先反馈组长。

## 3. 你优先修改的文件

```text
server/services/api.js
server/services/goalService.js
server/services/taskService.js
server/tests/core-flow.test.js
docs/API接口规范.md
```

可以少量修改：

```text
server/services/httpUtils.js
docs/操作手册.md
```

禁止修改：

```text
server/services/gameService.js
server/services/badgeService.js
server/services/aiService.js
client/styles.css
client/index.html
```

谨慎修改：

```text
server/services/database.js
```

只有新增必要字段或测试种子数据时才改，并必须在最终说明里写清楚。

## 4. 任务 B1：补全目标更新接口

新增或完善：

```text
PUT /api/goals/:id
```

请求体支持：

```json
{
  "title": "30 天掌握 Vue 项目开发",
  "description": "完成基础语法、组件通信和项目实战。",
  "category": "学习",
  "status": "active"
}
```

要求：

- 只能更新当前用户自己的目标。
- 标题不能为空。
- 返回格式：

```json
{
  "goal": {}
}
```

## 5. 任务 B2：补全目标删除接口

新增或完善：

```text
DELETE /api/goals/:id
```

要求：

- 只能删除当前用户自己的目标。
- 删除后不要导致任务列表接口报错。
- 可以保留任务并将 `goalId` 设为 `NULL`，因为数据库外键已配置 `ON DELETE SET NULL`。
- 返回格式：

```json
{
  "deleted": true
}
```

## 6. 任务 B3：补全任务编辑接口

新增或完善：

```text
PUT /api/tasks/:id
```

请求体支持：

```json
{
  "title": "更新后的任务标题",
  "description": "更新后的任务说明",
  "difficulty": "normal",
  "dueDate": "2026-07-10"
}
```

要求：

- 只能修改当前用户自己的任务。
- 不允许通过该接口修改 `xpReward`、`status`、`completedAt`。
- 不允许让已完成任务再次获得 XP。
- 返回格式：

```json
{
  "task": {}
}
```

## 7. 任务 B4：补充测试

在现有测试中补充验证：

```text
创建目标
→ 更新目标
→ 生成任务
→ 更新任务标题
→ 完成任务
→ 删除目标或任务
→ 核心流程仍通过
```

仍使用：

```bash
npm test
```

不要引入大型测试框架。

## 8. 任务 B5：更新 API 文档

更新：

```text
docs/API接口规范.md
```

补充：

```text
PUT /api/goals/:id
DELETE /api/goals/:id
PUT /api/tasks/:id
```

## 9. 提交前必须执行

```bash
npm test
npm run dev
```

可选接口冒烟测试：

```bash
curl -s http://localhost:3000/api/tasks -H 'X-User-Id: 1'
```

检查：

- `npm test` 通过。
- 旧接口没有坏。
- 完成任务后 XP 仍正常增加。

## 10. 提交和推送

```bash
git status --short
git add server docs/API接口规范.md docs/操作手册.md
git commit -m "feat: add goal and task crud APIs"
git push origin feature/backend-crud
```

## 11. 最终回复给成员的内容

完成后必须输出：

```text
完成内容：
新增/修改接口：
修改文件：
测试结果：
是否改动公共结构：
需要组长注意：
```
